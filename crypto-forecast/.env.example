# Optional: FRED API key for macro series (DXY, Gold, 10Y Treasury).
# Without it, the pipeline falls back to yfinance proxies automatically.
FRED_API_KEY=

# "regression" (predict next-day close) or "classification" (up/down direction)
TARGET_TYPE=regression
